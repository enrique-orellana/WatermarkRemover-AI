# Core AI/ML
# Backend-specific PyTorch installation is handled by the setup scripts.
transformers==4.57.0
diffusers==0.27.2
huggingface-hub>=0.34.0,<1.0
numpy<2

# Image/Video processing
opencv-python-headless>=4.8.0,<4.12.0
Pillow==9.5.0

# Inpainting core (iopaint installed separately with --no-deps to avoid resolver conflicts)

# GUI (PyWebview for brainrot HTML UI)
pywebview>=4.0

# Utilities
loguru
click
tqdm
psutil
pyyaml

# qt and gtk gui
PyGObject
qtpy
